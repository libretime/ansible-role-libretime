{{ libretime_log_dir }}/liquidsoap.log {
  su {{ libretime_user }} {{ libretime_user }}
  compress
  rotate 10
  size 1000k
  missingok
  notifempty
  sharedscripts
  postrotate
    systemctl kill --signal=SIGUSR1 libretime-liquidsoap >/dev/null 2>&1 || true
  endscript
}
